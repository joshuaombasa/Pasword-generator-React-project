import React from "react";

export default function Footer(props) {
    
    return (
        <footer className="paswords-display">
            <p>{props.password}</p>
            <p>{props.password}</p>
        </footer>
    )
}